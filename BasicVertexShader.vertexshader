#version 330 core

layout(location = 0)
in vec3 model_space_pos;

in vec4 color;

out vec4 colorV;

//gxl3d_ModelViewProjectionMatrix 
uniform mat4 MVP;

void main(){
	gl_Position = MVP * vec4(model_space_pos, 1);
	colorV = normalize(gl_position);
}